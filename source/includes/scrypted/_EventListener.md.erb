> Example

```javascript
light.on('OnOff', (eventSource, eventInterface, eventData) {
  if (eventData) {
    log.i('The light was turned on.');
  }
  else {
    log.i('The light was turned off.');
  }
});
```